packages:
  "framer-motion": "For premium smooth scroll animations and background effects"
  "react-hook-form": "For robust newsletter form state management"
  "@hookform/resolvers": "For Zod validation integration with the form"
  "react-helmet-async": "To manage SEO meta tags in the document head"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility to safely merge Tailwind classes"

images:
  - path: "artifacts/ethical-hacking/public/images/cyber-bg.png"
    description: "Dark navy abstract background with glowing cyan and green geometric grid lines, very subtle, deep space futuristic hacker aesthetic"
    aspect_ratio: "16:9"

notes:
  - The API hook `useSubscribeNewsletter` generated by Orval expects `{ data: { email: string } }` as mutation payload.
  - Using a dark aesthetic exclusively, matching the specified hex codes.
